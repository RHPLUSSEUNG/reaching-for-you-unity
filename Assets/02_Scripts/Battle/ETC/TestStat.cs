using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestStat : MonoBehaviour
{
    EntityStat stat;
    int skillID;
    List<EntityStat> statList;
    void Start()
    {
        stat = statList[0];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
